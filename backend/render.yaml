services:
  - type: web
    name: shree-drive
    env: node
    plan: free
    branch: main
    buildCommand: "cd backend && npm ci"
    startCommand: "node backend/server.js"
    envVars:
      - key: STATIC_DIR
        value: "private"
